; ============================================================================
;  Scavenger Sense - "Only sensing", the same answer the installer offers
;
;  Press a key and the world tells you what is worth taking. People included,
;  living ones only. No trails, and nothing touched about your interface.
;
;  This one changes only the settings its answer is about, exactly like the
;  installer fragment it mirrors - everything else stays where you have it.
;  It is not a way back to the shipped defaults - but it barely needs to
;  be: this answer is what the shipped defaults already are.
; ============================================================================

[Categories]
actor = true
actorFilter = living

[Tracks]
enabled = false
